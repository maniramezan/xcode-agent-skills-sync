# Gemini CLI Guidance

Read and follow the repository's canonical [AGENTS.md](AGENTS.md) before changing this repository.

Gemini-specific changes must preserve the root `gemini-extension.json` and the `commands/` TOML command format. Keep the command user-invoked and limited to refreshing `~/.gemini/skills` from the installed Xcode.
