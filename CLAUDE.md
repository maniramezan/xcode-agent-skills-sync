# Claude Code Guidance

Read and follow the repository's canonical [AGENTS.md](AGENTS.md) before changing this repository.

Claude-specific changes must keep `plugins/claude-xcode-skills-sync` and `.claude-plugin/marketplace.json` compatible with the documented Claude Code plugin format. Run `claude plugin validate plugins/claude-xcode-skills-sync` after editing the Claude plugin.
